
> "我们需要增强教育属性。
> 1. 在 `constants.js` 中扩充 `MBTI_DETAILS` 对象。
> * 包含 16 种类型的全称（如 '建筑师'）、简短描述、关键词。
> 
> 
> 2. 修改 `UserCard.jsx`：
> * 将卡片上的 MBTI 文字做成可点击的样式（加下划线或 hover 效果）。
> 
> 
> 3. 创建 `WikiModal`：
> * 点击 MBTI 文字时弹出，展示该类型的详细说明书。
> * 背景色使用该类型的对应色系。"
> 
> 
> 
> 
